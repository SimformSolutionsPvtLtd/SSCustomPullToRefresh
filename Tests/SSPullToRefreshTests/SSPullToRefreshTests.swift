import XCTest
@testable import SSPullToRefresh

final class SSPullToRefreshTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SSPullToRefresh().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
